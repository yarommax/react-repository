import styled from 'styled-components';
import { Link } from 'react-router-dom';

const StyledToolbarMenuItem = styled(Link)`
	padding: 10px 20px;
	color: black;
	text-decoration: unset;
	text-transform: uppercase;
`;

export default StyledToolbarMenuItem;
